# Configuración de Base de Datos (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/refrielectricos_db?schema=public"

# Configuración del Servidor
PORT=4000
JWT_SECRET="tu_secreto_jwt_super_seguro"

# Configuración de Rate Limiting
# Límite por segundo (short)
THROTTLE_SHORT_TTL=1000
THROTTLE_SHORT_LIMIT=10

# Límite por 10 segundos (medium)
THROTTLE_MEDIUM_TTL=10000
THROTTLE_MEDIUM_LIMIT=50

# Límite por minuto (long)
THROTTLE_LONG_TTL=60000
THROTTLE_LONG_LIMIT=100


# Configuración de Cloudinary (Para subida de imágenes)
CLOUDINARY_NAME="tu_cloud_name"
CLOUDINARY_API_KEY="tu_api_key"
CLOUDINARY_API_SECRET="tu_api_secret"

# ═══════════════════════════════════════════════════════════════
# EPAYCO PAYMENT GATEWAY CONFIGURATION
# ═══════════════════════════════════════════════════════════════
# Get these values from your ePayco dashboard:
# https://dashboard.epayco.co/configuration

# Customer ID (P_CUST_ID_CLIENTE from ePayco dashboard)
EPAYCO_CUST_ID="99999999"

# P_KEY for signature validation (from ePayco dashboard)
EPAYCO_P_KEY="weg9weg9wjg99239t92gh"

# Public API Key (from ePayco dashboard > API Keys)
EPAYCO_PUBLIC_KEY="your_epayco_public_key"

# Private API Key (from ePayco dashboard > API Keys)
EPAYCO_PRIVATE_KEY="your_epayco_private_key"

# Test mode: "true" for sandbox, "false" for production
EPAYCO_TEST="true"

# Base URLs for callbacks (adjust for your deployment)
BASE_URL_BACKEND="http://localhost:4000"
BASE_URL_FRONTEND="http://localhost:3000"

# ═══════════════════════════════════════════════════════════════
# GOOGLE OAUTH CONFIGURATION
# ═══════════════════════════════════════════════════════════════
# Get these from Google Cloud Console:
# https://console.cloud.google.com/apis/credentials

# Google OAuth Client ID
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"

# Google OAuth Client Secret (Backend only)
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ═══════════════════════════════════════════════════════════════
# EMAIL SERVICE CONFIGURATION (RESEND)
# ═══════════════════════════════════════════════════════════════
# Get API key from: https://resend.com/api-keys

# Resend API Key
RESEND_API_KEY="re_123456789"

# Email sender (must be a verified domain in Resend)
FROM_EMAIL="noreply@refrielectricos.com"

# Frontend URL for email links (verification, password reset)
FRONTEND_URL="https://frontend-production-4178.up.railway.app"

